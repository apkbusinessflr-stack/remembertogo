import { categories } from "@/lib/catalog"; export default function CategoryPage({ params, searchParams }:{ params:{ slug:string }, searchParams:{ country?:string } }){ const cat=categories.find(x=>x.slug===params.slug); const country=searchParams.country?.toUpperCase(); return (<main className="space-y-4"><h1>{cat ? cat.name : "Category"} {country ? `in ${country}` : ""}</h1><p className="opacity-80 text-sm">This is a category landing. In production, show featured lists/places for SEO.</p><ul className="list-disc list-inside opacity-80"><li>Featured lists (editorial)</li><li>Top places (by popularity)</li><li>Internal links to /lists/... and /place/...</li></ul></main>); }
