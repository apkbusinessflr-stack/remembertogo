import { NextResponse } from "next/server"; import { z } from "zod"; const schema=z.object({ place_id:z.number(), body:z.string().min(3).max(240), lang:z.string().default("en") }); export async function POST(req:Request){ const json=await req.json().catch(()=>null); const p=schema.safeParse(json); if(!p.success) return NextResponse.json({ error:p.error.flatten() },{status:400}); return NextResponse.json({ status:"pending" }); }
