import { ImageResponse } from 'next/og'; export const runtime='edge'; export const size={ width:1200, height:630 }; export const contentType='image/png';
export async function GET(req: Request){ const sp=new URL(req.url).searchParams; const name=sp.get('name') ?? 'Portugal • Beaches'; const v=Number(sp.get('v') ?? '3'); const t=Number(sp.get('t') ?? '12'); const pct=Math.round((v/Math.max(t,1))*100);
  return new ImageResponse((<div style={{width:'100%',height:'100%',display:'flex',flexDirection:'column',justifyContent:'center',alignItems:'center',background:'linear-gradient(135deg,#032b44,#0f6b84)',color:'#e6f0ff',fontSize:40,gap:12}}>
    <div style={{fontSize:72,fontWeight:800}}>{v}/{t} — {pct}%</div><div style={{opacity:.85}}>{name}</div><div style={{fontSize:22,opacity:.7}}>create yours → mappamou.example</div></div>),{...size}); }
